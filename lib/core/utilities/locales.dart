import 'dart:ui';

/// a class containing each supported [Locale]
class L18n {
  /// app asset locale paths
  static const String assetLocalePath = 'lang';

  /// all app supported locales
  static final locales = [
    const Locale('en'),
    const Locale('de'),
  ];
}
